<% if flash[:notice] %>
<div class="alert-message block-message <%= flash[:notice][:class] %>">
  <p>
    <%= flash[:notice][:message] %>
  </p>
</div>
<% end %>

<p>
  <a href="/service/create" class="btn success">Create new</a>
</p>
  
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @services.each do |service| %>
  <tr>
    <td><a href="/service/<%= service["_id"] %>"><%= service["title"] %></a></td>
    <td>
      <a href="/service/<%= service["_id"] %>/items" class="btn success">Items</a>
      <a href="/service/<%= service["_id"] %>/tests" class="btn success">Tests</a>
      <a href="/service/<%= service["_id"] %>/edit" class="btn">Edit</a>
      <a href="/service/<%= service["_id"] %>/delete" class="btn danger confirm">Delete</a>
    </td>
  </tr>
  <% end %>
  </tbody>
</table>